#include "main.h"
/**
 * _strspn - a function that the length of a prefix substring
 *
 * *Return: n bytes returned to the initial segment
 */
unsigned int _strspn(char *s, char *accept);
{
	int i = 0;

	for (; n > 0; i++)
	{
		s[i] = accept;
		n--;
	}
	return (s)
}
