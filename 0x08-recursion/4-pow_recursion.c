#include "main.h"
/**
 * _pow_recursion - a fuction that returns the value of x raised to the power of y
 * @x: the value
 * @y: the value to be doubled
 */
int _pow_recursion(int y, int x);
{
	if (y < 0)
		return (-1);
}
