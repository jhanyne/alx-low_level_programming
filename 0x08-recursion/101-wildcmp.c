#include "main.h"
/**
 * _wildcmp - a function that compares two strings and returns 1 if the strings can be considered identical, otherwise return 0
 * @s1: value of first string
 * @s2: value of second string
 *
 * Return: when two of the strings are compare
 */
{
	printf(s1 = s2)

		return (1);
}
